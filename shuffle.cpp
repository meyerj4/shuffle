#include "ShuffleApp.h"

// shuffle:
// Input an integer, N, shuffle the integers from 1..N, print the results
// Shuffles a list of numbers from 1..N

int main(int argc, char* argv[])
{
  ShuffleApp().run();
  return 0;
}

